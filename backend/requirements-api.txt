# ──────────────────────────────────────────────────────────────────────────────
# API-only dependencies — used by the Render deployment (backend.Dockerfile).
#
# Does NOT include ML packages (torch, insightface, CLIP, rembg, etc.).
# Those are installed via pyproject.toml in the local worker.Dockerfile.
# ──────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Task queue (Celery client — needed to enqueue jobs from the API)
celery[redis]==5.4.0
redis==5.0.4

# Vector store
qdrant-client==1.9.1

# Image processing (used in upload_router, asset_router, review_router)
Pillow==10.3.0
imagehash==4.3.1

# Cloud storage (Cloudflare R2 / AWS S3-compatible)
boto3==1.34.131

# Configuration & validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Logging
structlog==24.2.0

# HTTP client
httpx==0.27.0

# Utilities
aiofiles==23.2.1

# Monitoring
prometheus-fastapi-instrumentator==7.0.0
